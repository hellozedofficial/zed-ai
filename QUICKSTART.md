# Bedrock Chat - দ্রুত শুরু করার গাইড

## 🎯 দ্রুত সেটআপ (৫ মিনিটে)

### ১. প্রজেক্ট সেটআপ করুন

```bash
# Setup script চালান
./setup.sh
```

এটি স্বয়ংক্রিয়ভাবে:
- Virtual environment তৈরি করবে
- সমস্ত dependencies ইনস্টল করবে
- .env ফাইল তৈরি করবে

### ২. AWS Credentials যোগ করুন

`.env` ফাইলটি এডিট করুন এবং আপনার AWS credentials যোগ করুন:

```bash
AWS_ACCESS_KEY_ID=আপনার_access_key
AWS_SECRET_ACCESS_KEY=আপনার_secret_key
AWS_REGION=us-east-1
```

### ৩. অ্যাপ্লিকেশন চালান

```bash
./run.sh
```

### ৪. ব্রাউজারে খুলুন

```
http://localhost:5000
```

## 📋 AWS Bedrock সেটআপ

### AWS Console-এ Model Access সক্রিয় করুন:

1. AWS Console → Bedrock
2. "Model access" → "Manage model access"
3. নিচের মডেলগুলো select করুন:
   - Anthropic Claude 3 Sonnet ✅
   - Anthropic Claude 3 Haiku
   - Anthropic Claude 3 Opus
4. "Request model access" বাটনে ক্লিক করুন
5. অনুমতি পেতে ২৪-৪৮ ঘণ্টা সময় লাগতে পারে

### IAM User তৈরি করুন:

1. AWS Console → IAM → Users → Create user
2. User name দিন (যেমন: bedrock-chat-user)
3. Permissions:
   ```json
   {
       "Version": "2012-10-17",
       "Statement": [
           {
               "Effect": "Allow",
               "Action": [
                   "bedrock:InvokeModel",
                   "bedrock:InvokeModelWithResponseStream"
               ],
               "Resource": "*"
           }
       ]
   }
   ```
4. Access Key তৈরি করুন এবং সংরক্ষণ করুন

## 🎨 বৈশিষ্ট্যসমূহ

- ✅ ChatGPT-এর মতো UI/UX
- ✅ বাংলা ভাষা সম্পূর্ণ সাপোর্ট
- ✅ Multiple AI Models (Claude 3 Series)
- ✅ Conversation History
- ✅ Markdown Formatting
- ✅ Code Syntax Highlighting
- ✅ Responsive Design

## 🔧 কাস্টমাইজেশন

### মডেল পরিবর্তন করুন:

`.env` ফাইলে:
```bash
BEDROCK_MODEL_ID=anthropic.claude-3-haiku-20240307-v1:0
```

### Port পরিবর্তন করুন:

`.env` ফাইলে:
```bash
PORT=8000
```

## 📊 খরচ অনুমান

Claude 3 Sonnet (সুপারিশকৃত):
- Input: $3 প্রতি মিলিয়ন টোকেন
- Output: $15 প্রতি মিলিয়ন টোকেন

গড় চ্যাট সেশন (১০০টি মেসেজ): ~$0.50

## 🐛 সমস্যা সমাধান

### সমস্যা: "Access Denied"
**সমাধান:**
- AWS credentials সঠিক আছে কিনা চেক করুন
- Model access approved হয়েছে কিনা দেখুন
- IAM permissions চেক করুন

### সমস্যা: "Model not found"
**সমাধান:**
- Model ID সঠিক আছে কিনা দেখুন
- আপনার region-এ মডেল available কিনা চেক করুন
- Model access request submit করেছেন কিনা দেখুন

### সমস্যা: "Connection timeout"
**সমাধান:**
- Internet connection চেক করুন
- AWS region সঠিক আছে কিনা দেখুন
- Firewall settings চেক করুন

## 📚 উদাহরণ ব্যবহার

### প্রোগ্রামিং সাহায্য:
```
"Python-এ একটি REST API কিভাবে তৈরি করবো?"
"React-এ state management কিভাবে করবো?"
```

### সৃজনশীল লেখা:
```
"একটি sci-fi গল্প লিখো"
"আমার ব্যবসার জন্য একটি slogan তৈরি করো"
```

### শিক্ষা:
```
"Machine Learning কি?"
"Database normalization ব্যাখ্যা করো"
```

## 🚀 Production Deployment

### Gunicorn ব্যবহার করুন:

```bash
pip install gunicorn
gunicorn -w 4 -b 0.0.0.0:5000 app:app
```

### Docker ব্যবহার করুন:

```dockerfile
FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt
COPY . .
CMD ["python", "app.py"]
```

### Environment Variables:

Production-এ:
```bash
FLASK_DEBUG=False
PORT=80
```

## 📞 সাহায্য প্রয়োজন?

- GitHub Issues খুলুন
- README.md দেখুন
- AWS Bedrock Documentation পড়ুন

## 🎉 উপভোগ করুন!

এখন আপনি আপনার নিজের ChatGPT-এর মতো সিস্টেম ব্যবহার করতে পারবেন! 🚀
